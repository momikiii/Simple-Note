<view class="bg-white">
	<view class="bg-white" style="height:{{CustomBar}}px">
		<view class="hover animated {{MenuAnimate}}" wx:if="{{leftFlag}}">
			<view class="icon" style="top: {{CustomBar-35}}px">
				<view class="cuIcon-close animated " bindtap="close"></view>
			</view>
			<view class="txt" style="margin-top:{{CustomBar+40}}px">
				<view class="txt-item animated {{txtItemAnimate}} {{item.class}}" style="margin-bottom:{{CustomBar-10}}px"
					wx:for="{{HoverText}}" wx:key="id">{{item.text}}</view>
			</view>
		</view>
		<view class="icon-box">
			<view class="date">
				WRITING
			</view>
			<view class="leftIcon" bindtap="back">
				<view class="cuIcon-back"></view>
			</view>
		</view>
	</view>
	<date-info></date-info>

	<view class="container" style="height: {{changeHeight}}vh;">
		<editor id="editor" class="ql-container" placeholder="{{placeholder}}" bindstatuschange="onStatusChange"
			bindready="onEditorReady">
		</editor>
	</view>

	<view class="toolbar" catchtouchend="format" hidden="{{keyboardHeight > 0 ? false : true}}"
		style="bottom: {{isIOS ? keyboardHeight : 0}}px">
		<i class="iconfont icon-charutupian" catchtouchend="insertImage"></i>
		<i class="iconfont icon-format-header-2 {{formats.header === 2 ? 'ql-active' : ''}}" data-name="header"
			data-value="{{2}}"></i>
		<i class="iconfont icon-format-header-3 {{formats.header === 3 ? 'ql-active' : ''}}" data-name="header"
			data-value="{{3}}"></i>
		<i class="iconfont icon-zitijiacu {{formats.bold ? 'ql-active' : ''}}" data-name="bold"></i>
		<i class="iconfont icon-zitixieti {{formats.italic ? 'ql-active' : ''}}" data-name="italic"></i>
		<i class="iconfont icon-zitixiahuaxian {{formats.underline ? 'ql-active' : ''}}" data-name="underline"></i>
		<i class="iconfont icon--checklist" data-name="list" data-value="check"></i>
		<i class="iconfont icon-youxupailie {{formats.list === 'ordered' ? 'ql-active' : ''}}" data-name="list"
			data-value="ordered"></i>
		<i class="iconfont icon-wuxupailie {{formats.list === 'bullet' ? 'ql-active' : ''}}" data-name="list"
			data-value="bullet"></i>
	</view>
</view>